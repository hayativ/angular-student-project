{
  "name": "variant-b",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm run install:frontend && npm run install:api",
    "install:frontend": "cd frontend && npm install",
    "install:api": "cd mock-api && npm install",
    "start": "cd frontend && npm run all",
    "start:frontend": "cd frontend && npm start",
    "start:api": "cd mock-api && npm start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
